import styled from 'styled-components';

const Box = styled.div`
    height: 650px;
    width: 1200px;
    padding: 60px;
    align-self: center;
    margin: 30px auto;
    display: flex;
    flex-direction: row;
    align-items: center;
`;

export default Box;
